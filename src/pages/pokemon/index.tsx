import type { FC } from 'react';
import { Box } from '@mui/material';
import { PokemonDetail } from 'src/sections/pokemon';

const Pokemon: FC = () => (
  <Box>
    <PokemonDetail />
  </Box>
);

export default Pokemon;
